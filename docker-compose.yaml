name: POC-Project

include:
  - ./2.kafka/docker-compose.yaml

services:
  producer:
    build: ./1.signal-generator
    container_name: signal-generator
    volumes:
      - ./1.signal-generator/logs:/app/logs
    depends_on:
      kafka-1:
        condition: service_healthy
  
  # minio-bronze:
  #   image: minio/minio
  #   container_name: minio-bronze
  #   command: server /data --console-address ":9001"
  #   ports:
  #     - "9000:9000"  # You access API at localhost:9000
  #     - "9001:9001"  # You access console at localhost:9001
  #   networks:
  #     - backend
  #   volumes:
  #     - bronze-minio-data:/data # if you don't add volumes at the bottom it is a bind voume
    
  #   environment:
  #     - MINIO_ROOT_USER=${MINIO_ROOT_USER}
  #     - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}

  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 5s

  # minio-gold:
  #   image: minio/minio
  #   container_name: minio-gold
  #   command: server /data --console-address ":9001"
  #   ports:
  #     - "9010:9000"  # You access API at localhost:9000
  #     - "9011:9001"  # You access console at localhost:9001
  #   networks:
  #     - backend
  #   volumes:
  #     - gold-minio-data:/data # if you don't add volumes at the bottom it is a bind voume
    
  #   environment:
  #     - MINIO_ROOT_USER=${MINIO_ROOT_USER}
  #     - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}

  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 5s

  # injestion-app:
  #   build: ./injestion-app
  #   container_name: injestion-app
  #   env_file:
  #     - .env
  #   depends_on:
  #     minio-bronze:
  #       condition: service_healthy
  #   networks:
  #     - backend
  #   volumes:
  #     - ./injestion-app/Download:/app/Download
  #     - ./injestion-app/Upload:/app/Upload

  # trasfnormation-app:
  #   build: ./trasfnormation-app
  #   command: sleep infinity #tail -f /dev/null
  #   container_name: trasfnormation-app
  #   volumes:
  #     - type: bind
  #       source: ./trasfnormation-app
  #       target: /app
  #   env_file:
  #     - .env
  #   depends_on:
  #     minio-bronze:
  #       condition: service_healthy
  #     minio-gold:
  #       condition: service_healthy
  #   networks:
  #     - backend

  # airflow:
  #   image: xxx
  #   container_name: airflow
  #   volumes:
  #     - type: bind
  #       source: /airflow/config
  #       target: /opt/airflow/config

  #     - type: bind
  #       source: /airflow/dags
  #       target: /opt/airflow/dags

  #     - type: bind
  #       source: /airflow/logs
  #       target: /opt/airflow/logs

  #     - type: bind
  #       source: /airflow/plugins
  #       target: /opt/airflow/plugins
  #   depends_on:
  #     - minio-bronze:
  #       condition: service_healthy
  #     - minio-gold:
  #       condition: service_healthy
  #   networks:
  #     - backend

networks:
  backend:

volumes:
  bronze-minio-data:  # now it it is named volume managed by docker
  gold-minio-data:
